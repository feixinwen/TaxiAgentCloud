apiVersion: v1
kind: Service
metadata:
  name: taxiagent-rag-service
  namespace: taxiagent
spec:
  type: ClusterIP
  selector:
    app: taxiagent-rag-service
  ports:
    - name: http
      port: 8086
      targetPort: http
