apiVersion: v1
kind: Service
metadata:
  name: taxiagent-auth-service
  namespace: taxiagent
spec:
  type: ClusterIP
  selector:
    app: taxiagent-auth-service
  ports:
    - name: http
      port: 8082
      targetPort: http
