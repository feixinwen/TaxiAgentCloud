apiVersion: v1
kind: Service
metadata:
  name: taxiagent-registry
  namespace: registry
spec:
  type: NodePort
  selector:
    app: taxiagent-registry
  ports:
    - name: registry
      port: 5000
      targetPort: 5000
      nodePort: 30500
