services:
  mongo:
    image: mongo:8.0
    container_name: taxiagent-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
