apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: jenkins-buildkit-config
    namespace: jenkins
    files:
      - buildkitd.toml
